type Query {
    products: [Product]
    productsByPrice(min: Float!, max: Float!): [Product] 
}

type Product {
    id: ID!
    description: String!
    review: [Review]
    price: Float!
}

type Review {
    rating: Int!
    comment: String
}